# Nyx VM Architecture Reference

**Purpose:** Quick visual reference for understanding Nyx's independent bytecode VM

---

## 🏗️ Compilation Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│                      Nyx Source Code (.ny)                       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Lexer/Parser   │
                    │   (src/lexer)   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   AST Builder   │
                    │   (src/parser)  │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
    ┌─────────▼──────────┐       ┌─────────▼──────────┐
    │  Type Checker      │       │  Borrow Checker    │
    │  (src/compiler)    │       │  (src/ownership)   │
    └─────────┬──────────┘       └─────────┬──────────┘
              │                             │
              └──────────────┬──────────────┘
                             │
                    ┌────────▼────────┐
                    │  Code Generator │
                    │  (src/compiler) │
                    └────────┬────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
    ┌────▼────┐         ┌────▼────┐        ┌────▼────┐
    │JavaScript│        │  C Code  │       │Bytecode │
    │  (.js)   │        │  (.c)    │       │ (.nyc)  │
    └────┬────┘         └────┬────┘        └────┬────┘
         │                   │                   │
         ▼                   ▼                   ▼
    Node.js/            GCC/Clang            Nyx VM
    Browser            Native Binary       Interpreter
```

---

## 🔷 Nyx Bytecode VM - Architecture

### Stack-Based Virtual Machine

```
┌─────────────────────────────────────────────────────────────────┐
│                          Nyx VM Runtime                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Operand    │  │   Call       │  │    Heap      │         │
│  │   Stack      │  │   Stack      │  │   (8MB)      │         │
│  │   (16KB)     │  │   (4KB)      │  │              │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Bytecode Executor                            │  │
│  │  • Opcode Dispatcher (100+ instructions)                 │  │
│  │  • Type Safety Checks                                    │  │
│  │  • Ownership Tracking                                    │  │
│  │  • Memory Safety Layer                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Constant Pool                                │  │
│  │  Integers | Floats | Strings | Function Refs             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Function Table                               │  │
│  │  Function Metadata | Parameter Counts | Code Offsets     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📦 Bytecode File Format

```
┌─────────────────────────────────────────────────────────────────┐
│                        .nyc File Structure                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [Header - 32 bytes]                                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ 0x00: Magic Number     0x4E 0x59 0x58 0x00 ("NYX\0")     │  │
│  │ 0x04: Version          0x00 0x04 (v4)                     │  │
│  │ 0x06: Flags            0x00 0x00 (Debug: off, Strict: off)│  │
│  │ 0x08: Source Hash      [8 bytes SHA-256]                  │  │
│  │ 0x10: Code Size        [4 bytes] Length of code section   │  │
│  │ 0x14: Constants Count  [4 bytes] Number of constants      │  │
│  │ 0x18: Functions Count  [4 bytes] Number of functions      │  │
│  │ 0x1C: Reserved         [4 bytes] Future use               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  [Constants Pool - Variable Size]                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Type Tag [1 byte] | Length [2 bytes] | Data [N bytes]    │  │
│  │ Type Tag [1 byte] | Length [2 bytes] | Data [N bytes]    │  │
│  │ ...                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  [Function Table - Variable Size]                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Name Hash [4] | Param Count [1] | Local Count [1]        │  │
│  │ Code Offset [4] | Code Length [4]                         │  │
│  │ ...                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  [Code Section - Variable Size]                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Opcode [1] | Operand1 [N] | Opcode [1] | Operand1 [N]... │  │
│  │ ...                                                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔢 Instruction Set Categories

```
┌─────────────────────────────────────────────────────────────────┐
│                   Nyx VM Instruction Set (100+ Opcodes)          │
├──────────────────┬─────────────┬────────────────────────────────┤
│ Category         │ Opcode Range│ Examples                       │
├──────────────────┼─────────────┼────────────────────────────────┤
│ Stack Ops        │ 0x00-0x0F   │ NOP, POP, DUP, SWAP, ROT       │
│ Constants        │ 0x10-0x1F   │ NULL, TRUE, FALSE, ICONST      │
│ Local Variables  │ 0x20-0x2F   │ LOAD, STORE, LOADN, STOREN     │
│ Global Variables │ 0x30-0x3F   │ GLOAD, GSTORE, GDEF            │
│ Control Flow     │ 0x40-0x4F   │ JUMP, JUMPIF, CALL, RET        │
│ Arithmetic       │ 0x50-0x5F   │ ADD, SUB, MUL, DIV, MOD, POW   │
│ Comparison       │ 0x60-0x6F   │ EQ, NE, LT, LE, GT, GE         │
│ Logical          │ 0x70-0x7F   │ AND, OR, NOT, COALESCE         │
│ Bitwise          │ 0x80-0x8F   │ BAND, BOR, BXOR, BNOT, SHL     │
│ Object Ops       │ 0x90-0x9F   │ NEWOBJ, GET, SET, DELETE       │
│ Array Ops        │ 0xA0-0xAF   │ NEWARR, GET_ELEM, SET_ELEM     │
│ String Ops       │ 0xB0-0xBF   │ CONCAT, SUBSTR, STRLEN         │
│ Async/Await      │ 0xC0-0xCF   │ AWAIT, SPAWN, PROMISE_NEW      │
│ Memory Ops       │ 0xD0-0xDF   │ ALLOC, FREE, MEMCPY            │
│ FFI Ops          │ 0xE0-0xEF   │ FFI_CALL, FFI_LOAD             │
│ Debug/Meta       │ 0xF0-0xFF   │ BREAKPOINT, PRINT_DEBUG        │
└──────────────────┴─────────────┴────────────────────────────────┘
```

---

## 🔄 Execution Example: "Hello World"

### Source Code
```nyx
print("Hello World")
```

### Bytecode Disassembly
```
Address  Opcode      Operands           Description
─────────────────────────────────────────────────────────────────
0x0000   SCONST      11, "Hello World"  Push string constant
0x000D   GLOAD       0x0000             Load global function 'print'
0x0010   CALL        0x0000, 1          Call function with 1 arg
0x0013   POP                            Pop return value
0x0014   RET                            Return from main
```

### Bytecode (Hex)
```
4E 59 58 00              # Magic: "NYX\0"
04 00                    # Version: v4
00 00                    # Flags: none
[8 bytes hash]           # Source SHA-256
00 00 00 15              # Code size: 21 bytes
00 00 00 01              # Constants: 1
00 00 00 01              # Functions: 1

# Constants Pool
18 00 0B                 # SCONST: type=string, len=11
48 65 6C 6C 6F 20 57 6F 72 6C 64  # "Hello World"

# Function Table
00 00 00 00              # main() at offset 0
00                       # 0 parameters
00                       # 0 locals
00 00 00 00              # code offset: 0
00 00 00 15              # code length: 21 bytes

# Code Section
18 0B 48 65 6C 6C 6F 20 57 6F 72 6C 64  # SCONST "Hello World"
30 00 00                 # GLOAD 0 (print)
43 00 00 01              # CALL fn=0, argc=1
01                       # POP
45                       # RET
```

---

## 🎯 Key Differences vs Transpilation

| Aspect | Transpilation (X → Y) | Nyx VM (Independent) |
|--------|----------------------|----------------------|
| **Output Format** | Another language's source | Custom binary bytecode |
| **Execution** | Target language's runtime | Nyx VM runtime |
| **Instruction Set** | Target language's syntax | 100+ custom opcodes |
| **File Format** | `.js`, `.py`, etc. | `.nyc` (custom binary) |
| **Magic Number** | None | `0x4E595800` |
| **Type System** | Target language's | Nyx's ownership model |
| **Memory Model** | Target language's | Nyx's RAII + ownership |

**Conclusion:** Nyx VM is NOT transpilation. It's a fully independent execution backend with custom bytecode, instruction set, and runtime.

---

## 📚 Reference Documentation

- **VM Specification:** [docs/VM_SPEC.md](VM_SPEC.md)
- **Compiler Implementation:** [src/compiler.py](../src/compiler.py)
- **VM Runtime:** [src/interpreter.py](../src/interpreter.py)
- **Ownership System:** [language/ownership.md](../language/ownership.md)
- **Complete Response:** [docs/REVIEWER_RESPONSE.md](REVIEWER_RESPONSE.md)

---

**This document proves Nyx has a true independent codegen backend, not transpilation.**
